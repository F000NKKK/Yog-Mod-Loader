[package]
name = "yog-hot-reload"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Safe dlopen/dlclose swap primitives for reloading a mod's native library without dangling references into retired code."

[dependencies]
libloading.workspace = true
