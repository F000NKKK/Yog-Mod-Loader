[package]
name = "yog-event"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
license = "MIT OR Apache-2.0"
description = "Yog event system — pub/sub for game/server events."

[dependencies]
yog-core = "0.1"