[package]
name = "yog-command"
version.workspace = true
edition.workspace = true
license = "MIT OR Apache-2.0"
description = "Yog command domain — types for registering chat commands."

[dependencies]
