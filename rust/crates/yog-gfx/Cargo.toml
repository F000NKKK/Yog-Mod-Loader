[package]
name = "yog-gfx"
version.workspace = true
edition.workspace = true
license = "MIT OR Apache-2.0"
description = "Yog graphics subsystem — low-level GPU pipeline access for mods."

[dependencies]
yog-abi = { path = "../yog-abi" }
