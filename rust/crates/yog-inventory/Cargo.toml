[package]
name = "yog-inventory"
version = "0.2.0"
edition.workspace = true
authors.workspace = true
license = "MIT OR Apache-2.0"
description = "Yog inventory framework — real Container/Menu screens (vanilla-style or fully custom) for Yog mods"

[dependencies]
yog-abi = "0.16"
