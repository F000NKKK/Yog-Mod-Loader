[package]
name = "yog-example-mod"
version.workspace = true
edition.workspace = true
# Permissive so people can copy it as a starting template for their own mods.
license = "MIT OR Apache-2.0"
description = "Example Yog mod demonstrating the API."

[dependencies]
yog-api = { path = "../yog-api" }
