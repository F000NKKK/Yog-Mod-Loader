[package]
name = "yog-abi"
version = "0.14.0"
edition.workspace = true
authors.workspace = true
license = "MIT OR Apache-2.0"
description = "Yog stable C ABI — the shared contract between mods and the runtime."
