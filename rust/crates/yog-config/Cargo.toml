[package]
name = "yog-config"
version.workspace = true
edition.workspace = true
license = "MIT OR Apache-2.0"
description = "Yog mod configuration — TOML-style key/value config files."

[dependencies]
