[package]
name = "yog-registry"
version.workspace = true
edition.workspace = true
license = "MIT OR Apache-2.0"
description = "Yog content registration — declare custom items and blocks."

[dependencies]
